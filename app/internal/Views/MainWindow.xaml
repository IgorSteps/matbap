<Window x:Class="app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:app"
        mc:Ignorable="d"
        Title="Matbap" Height="450" Width="320.07">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Text="{Binding Expression}" Margin="10,5,10,200" Grid.RowSpan="3"/>
        <TextBlock Grid.Row="2" Text="{Binding Response}" FontSize="10" Margin="10,232,10,130"/>
        <Button Grid.Row="2" Content="Evaluate" Command="{Binding InterpretCmd}" Margin="10,331,10,10"/>

    </Grid>
</Window>
