<Window x:Class="app.PlotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:app"
        mc:Ignorable="d"
        Title="Plot" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Label Content="Slope:" Margin="5"/>
            <!--UpdateSourceTrigger=PropertyChanged makes the binding updates on the PlotViewModel as soon as the text in the boxes changes.-->
            <TextBox Width="50" Margin="5" Text="{Binding Slope, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="Intercept:" Margin="5"/>
            <!--UpdateSourceTrigger=PropertyChanged makes the binding updates on the PlotViewModel as soon as the text in the boxes changes.-->
            <TextBox Width="50" Margin="5" Text="{Binding Intercept, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        
        <oxy:PlotView Grid.Row="1" Model="{Binding PlotModel}" />
    </Grid>
</Window>
